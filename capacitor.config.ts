import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.horodyski.teataster',
  appName: 'Tea Taster',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
